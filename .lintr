linters: linters_with_defaults(line_length_linter = NULL, assignment_linter = assignment_linter("="),indentation_linter = indentation_linter(4), object_name_linter = NULL)

linters <- list()     # an empty list means “run no linters at all”
encoding <- "UTF-8"